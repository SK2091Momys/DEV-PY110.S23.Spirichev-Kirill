С помощью итератора `repeat` 4 раза повторить переменную `a`

```python
import itertools

itertools.repeat(elem)
```
 – возвращает один и тот же элемент бесконечное число раз либо указаннное количество раз.