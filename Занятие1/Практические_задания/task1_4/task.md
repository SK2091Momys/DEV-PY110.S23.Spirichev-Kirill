Написать функцию, которая с помощью метода строки `upper`, переводила строки в верхний регистр.  

Методы строки можно использовать двумя способами.
```python
"word".upper()  # "WORD"
str.upper("word")  # "WORD"
```